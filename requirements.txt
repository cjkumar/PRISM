# PRISM - Policy Reasoning Integrated Sequential Model
# Core dependencies

# Document processing
PyMuPDF>=1.23.0                    # PDF text extraction (Agent 1 lightweight mode)
numpy>=1.24.0                      # Array operations
Pillow>=10.0.0                     # Image processing

# Embeddings and semantic similarity (Page References, Agent 3)
sentence-transformers>=2.2.0       # all-MiniLM-L6-v2 embeddings
torch>=2.0.0                       # PyTorch backend

# Clustering (Page Reference adaptive thresholds)
scikit-learn>=1.3.0                # K-Means clustering

# API client (Agent 2 inference)
openai>=1.0.0                      # OpenAI-compatible API client
requests>=2.31.0                   # HTTP requests fallback

# Data processing
pandas>=2.0.0                      # DataFrame operations (visualization/export)

# === Optional dependencies ===

# Full Agent 1 (Qwen2.5-VL-72B) - requires GPU
# transformers>=4.37.0             # HuggingFace transformers
# accelerate>=0.25.0               # Model parallelism

# Image preprocessing (Agent 1)
# opencv-python>=4.8.0             # Image preprocessing pipeline

# PDF to image conversion (Agent 1 alternative)
# pdf2image>=1.16.0                # Poppler-based PDF rendering
